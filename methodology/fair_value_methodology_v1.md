# Rally Terminal Fair Value Methodology v1

This methodology is a provisional deterministic framework for asset-level research-library infrastructure. It is not an appraisal standard and must not be presented as definitive fair value.

The v1 engine uses eligible documented comparable sales, transparent adjustment multipliers from versioned YAML configuration, and robust weighted quantiles. Insufficient evidence returns `insufficient_evidence` rather than a confident official range.
